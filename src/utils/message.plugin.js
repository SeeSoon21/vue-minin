export default {
    install: (Vue, options) => {
        /*Vue.prototype.$mesage = function(html) {
            return html;
        }

        Vue.prototype.$error = function(html) {
            M.toast({html: `[Ошибка]: ${html}`})
        }*/
    }    
}

